同步：当请求方发送请求给服务端，服务端在没有处理结果返回时，请求方将处于等待的状态。
异步：当请求方发送请求给服务端，请求方无需等待该请求的结果，可以等服务端处理完后再去得到结果。

同步阻塞：客户端一直等待服务端的处理结果，在没得到处理结果前一直处于等待状态。
同步非阻塞：客户端将请求发给服务器后，不必一直等待，只要过一段时间后主动向服务端查询请求的处理结果。
异步阻塞：客户端一直等待服务端的处理结果，当得到结果时服务端会主动告诉客户端。
异步非阻塞：客户端将请求发给服务器后，不必一直等待，过一段时间后服务端会主动向客户端发送处理结果。